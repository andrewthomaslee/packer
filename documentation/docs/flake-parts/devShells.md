# flake-parts/devShells.nix

Development environment definitions.

## Overview
Configures `nix develop` shells with required tools and hooks.

## Shells
- `default`: Main development shell including `bun`, `clan-cli`, and repository hooks.
